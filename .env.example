# Tauri updater signing key. Accepts either the key file's contents or a path to
# the .key file (generate with: npm run tauri -- signer generate -w pear.key).
TAURI_SIGNING_PRIVATE_KEY=C:\Users\you\.tauri\pear.key
TAURI_SIGNING_PRIVATE_KEY_PASSWORD=your-key-password
